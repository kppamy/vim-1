   passwd root
   sudo passwd root
   sudo vi /etc/resolv.conf 
   sudo apt-get update
   sudo apt-get install vim
   sudo visudo 
   sudo vim /etc/sudoers
   sudo apt-get install python3.5
   sudo apt-get install pip3
   sudo apt-get update
   sudo apt-get install pip3
   sudo apt-get install python3-pandas
   sudo apt-get install python3-matplotlib
   sudo apt-get install python3-requests
   sudo apt-get install git
   ssh-keygen -t rsa -b 4096 -C "zhhcay@163.com"
   cat .ssh/id_rsa.pub 
   ssh -T git@github.com
   mkdir programs
   mkdir python
   cd programs/
   git clone https://github.com/kppamy/vim-1.git
   cp .bashrc ~/
   source ~/.bashrc 
   mkdir python
   cd python/
   git clone https://github.com/kppamy/tushare.git
   cd tushare/
   gco -b money origin/money
   cd python/
   git clone https://github.com/kppamy/stockholm.git
   cd stockholm/
   gco -b money origin/money
   cd pydata/
   mv stockholm/pydata/
   sudo apt-get install python3-sqlalchemy
   sudo apt-get install sqlite3
   sudo apt-get install ipython3
   pip3 install pandas_datareader
   pip3 install --upgrade pip
 git config --global user.email "zhhcay@163.com"
git config --global user.name "kppamy"

